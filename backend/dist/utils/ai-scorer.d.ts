export declare class AILeadScorer {
    /**
     * Rule-based scoring logic to simulate AI lead prioritization
     * @param lead - The lead object from Prisma
     * @returns score from 0-100
     */
    static calculateScore(lead: any): number;
}
